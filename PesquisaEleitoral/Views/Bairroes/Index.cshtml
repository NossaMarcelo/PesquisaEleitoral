@model IEnumerable<PesquisaEleitoral.Models.Bairro>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BairroNome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BairroNome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BairroId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BairroId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BairroId })
        </td>
    </tr>
}

</table>
